ENCODE_HTML
    ENCODE_HTML
  ENCODE_HTML
  ENCODE_HTML
    ENCODE_HTML
  ENCODE_HTML

# المترجم التلقائي لـ GitHub

يقوم إجراء GitHub تلقائيًا بإنشاء ودفع الإصدارات المترجمة من ملف README.md الخاص بك بناءً على اللغات المدعومة.

## الاستخدام

لاستخدام هذا الإجراء، قم بإنشاء ملف سير عمل (على سبيل المثال، `.github/workflows/translate.yml`) في مستودعك بالمحتوى التالي:

ENCODE_BLOCK

استبدل LANGS بقائمة مفصولة بفواصل باللغات التي تريد إنشاؤها.
اللغات المتاحة للترجمة:
ENCODE_BLOCK

## إعدادات

يمكنك تكوين هذا الإجراء باستخدام المدخلات التالية:

- `اللغات`: قائمة مفصولة بفواصل باللغات المراد إنشاؤها.
- `github_token`: رمز GitHub المميز للمصادقة.

## مثال

على سبيل المثال، إذا كنت تريد إنشاء ملفات README للغات الصربية والإيطالية والإنجليزية، فسيبدو التكوين الخاص بك كما يلي:

ENCODE_BLOCK

## رخصة

تم ترخيص هذا المشروع بموجب ترخيص MIT - راجع ملف [LICENSE](LICENSE) للحصول على التفاصيل.